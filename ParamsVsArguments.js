function square(num){//params -> the variables that are defined when the function is declared 
    console.log(num *num);
}

square(5);//Arguments -> The values that are declared within a function when the function is called